rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo app — open access. Restrict these rules before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
